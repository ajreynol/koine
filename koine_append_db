#!/usr/bin/env python3
"""Retired root entry point; use bug_db_manager/koine_append_db.

This remains a tombstone for consumers probing the root path. Current callers
should probe and invoke the implementation in bug_db_manager and choose their
koine dependency pin themselves.
"""

import os
import sys

MOVED = os.path.join(os.path.dirname(os.path.abspath(__file__)),
                     "bug_db_manager", "koine_append_db")
MESSAGE = (
    "koine_append_db is at bug_db_manager/koine_append_db.\n"
    f"    use: {MOVED}\n"
    "This root entry point is a tombstone and does not append.\n"
    "Update your consumer's probe and invocation path together."
)

if __name__ == "__main__":
    print(MESSAGE, file=sys.stderr)
    sys.exit(2)

raise ImportError(MESSAGE)
