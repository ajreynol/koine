{
  "what": "The commands install_eo_cmd puts on a person's path. koine maintains these under role R35 in kanon's docs/roles.md -- their text, their options, and what they ask an assistant to do. What joining costs and what a member is held to is R4's and stays with the office: this repository maintains the program that states the rule and has no standing to change the rule.",
  "history": "Both were kanon's prompts/join_eo and prompts/init_eo until 2026-09-17, when kanon deleted them and R35 was created to hold them here. They were briefly stored as unmodifiable copies; that arrangement ended with the handover, and koine now writes them.",
  "commands": [
    {
      "name": "eo_join",
      "what": "join the Eunoia ecosystem, run inside the repository that is joining",
      "forms": [
        "eo_join",
        "eo_join --associate",
        "eo_join --soft",
        "eo_join --soft --affiliated"
      ],
      "kind": "prompt"
    },
    {
      "name": "eo_init",
      "what": "start a tool: write a README saying what it is for, complying with nothing",
      "forms": [
        "eo_init new",
        "eo_init from-child <path>"
      ],
      "kind": "prompt"
    },
    {
      "name": "eo_status",
      "what": "who is in the Eunoia ecosystem and on what footing, as the register says it. Reading, not auditing",
      "forms": [
        "eo_status",
        "eo_status --check",
        "eo_status --children"
      ],
      "kind": "program",
      "needs": "register"
    },
    {
      "name": "koine_append_db",
      "path": "bug_db/koine_append_db",
      "what": "add a run's new bugs to the database of every bug a tool has found. For a person at a terminal: a pinned consumer resolves it through its own lock and must not take it off PATH, which would lose the pin",
      "forms": [
        "koine_append_db <new bugs> <bug database>"
      ],
      "kind": "program"
    },
    {
      "name": "eo_respond",
      "what": "answer one topic another tool has addressed to you. Run in your own repository; naming the topic is what authorises answering it, and a run without one is refused",
      "forms": [
        "eo_respond <name> <Dn>"
      ],
      "kind": "prompt"
    },
    {
      "name": "eo_housekeeping",
      "what": "bring a repository up to date: documentation made true of the tree, the topics other tools addressed to it answered, bugs in its own tooling fixed, and CI green. Two paragraphs that name where the rules are rather than paraphrasing them",
      "forms": [
        "eo_housekeeping",
        "eo_housekeeping --report"
      ],
      "kind": "prompt"
    }
  ],
  "convention": "Every installed command carries a prefix saying whose it is, because the name is claimed inside somebody else's process and on their PATH. `eo_` is the ecosystem's commands, which run inside a tree that is not this one. `koine_` is this repository's own. install_eo_cmd refuses anything else.",
  "kinds": "`prompt` hands context to an assistant and takes --show-prompt, which prints what it would hand one and runs nothing. `program` does the work itself.",
  "register": "A command marked `needs: register` has the president's ecosystem.json baked into it when install_eo_cmd copies it, with the commit and date it was taken. That makes it usable away from the president's tree without ever presenting a snapshot as the live register.",
  "ground_truth": "This file is the ground truth for what these commands are and what each is for. The copy is the table in eo_cmd/README.md, and test_the_readme_table_agrees_with_the_manifest in tests/test_eo_cmd.py is what compares the two -- it decides that the same names appear in both and that each has somewhere it is written up, and it cannot decide whether a description is still true of the behaviour. The one claim `kinds` makes that a program can check, that only a prompt takes --show-prompt, is checked beside it."
}
