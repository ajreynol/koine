# The commit of anoieu whose policy checker this repository is checked by.
#
# A pin is a choice, not a fact: moving it changes what decides whether this
# tree complies. The ecosystem's rule is that a pin only moves to a commit
# where the other project's CI is green, which is what `eo_bump` enforces.
#
# The checker is `scripts/policy_check.py` and the contract asked for is `1`.
# Both are named in `.github/workflows/anoieu.yml`, and anoieu's checker
# contract undertakes to keep that entry point resolving across a file move --
# so the path and this pin no longer have to move together, which they did
# while the pin predated the move of the checker out of `tools/`.
154228a40d21584b95f4029742ccc8f432ea87f5
